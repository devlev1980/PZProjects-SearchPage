import { CardsByDepartmentPipe } from './cards-by-department.pipe';

describe('CardsByDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsByDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
