import { SearchByLocationPipe } from './search-by-location.pipe';

describe('SearchByLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
