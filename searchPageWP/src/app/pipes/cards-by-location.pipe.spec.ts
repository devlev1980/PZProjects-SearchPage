import { CardsByLocationPipe } from './cards-by-location.pipe';

describe('CardsByLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsByLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
