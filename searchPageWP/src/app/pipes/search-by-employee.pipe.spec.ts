import { SearchByEmployeePipe } from './search-by-employee.pipe';

describe('SearchByEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
