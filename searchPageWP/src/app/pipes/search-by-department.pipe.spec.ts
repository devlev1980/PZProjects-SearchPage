import { SearchByDepartmentPipe } from './search-by-department.pipe';

describe('SearchByDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
