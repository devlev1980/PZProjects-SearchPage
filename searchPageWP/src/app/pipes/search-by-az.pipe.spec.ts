import { SearchByAZPipe } from './search-by-az.pipe';

describe('SearchByAZPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByAZPipe();
    expect(pipe).toBeTruthy();
  });
});
